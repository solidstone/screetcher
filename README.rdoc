== README

Welcome to Screetcher, an app that I like to imagine is very similar to a pre-beta version of Twitter.

to get started Run:
bundle install

This database is postgresql. Run:
rake db:create
bundle exec rake db:migrate

to get database to current version

This project uses rspec and capybara for testing. To test run: 
bundle exec rspec


* Deployment instructions

* ...

